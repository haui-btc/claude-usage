services:
  claude-usage:
    build: .
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ~/.claude:/root/.claude
    environment:
      HOST: "0.0.0.0"
      PORT: "8080"
      SCAN_INTERVAL: "30"
